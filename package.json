{
  "private": true,
  "scripts": {
    "prepare": "nitropack prepare",
    "dev": "bun --bun nitropack dev",
    "build": "bun nitro build",
    "preview": "bun .output/server/index.mjs",
    "start": "bun .output/server/index.mjs",
    "lint": "bun --bun eslint .",
    "lint:fix": "bun --bun eslint --fix .",
    "upgrade": "bun upgrade --canary && bun --version && bunx taze major -w && bun i"
  },
  "dependencies": {
    "@types/bun": "^1.0.2",
    "nanotar": "^0.1.1",
    "nitropack": "latest",
    "xterm": "^5.3.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "@nuxtjs/eslint-config-typescript": "^12.1.0"
  }
}